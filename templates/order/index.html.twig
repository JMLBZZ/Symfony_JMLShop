{% extends 'base.html.twig' %}

{% block body %}
    <div class="container my-5 small-container">

        <h1 class="mb-5">Livraison</h1>
        
        {{ form_start (deliveryForm) }}
            {{ form_row(deliveryForm.addresses) }}
            <a href="{{ path("app_account_address_form")}}" class="btn btn-light"><i class="bi bi-plus-circle-fill"></i></a>
            <hr class="my-5">
            {{ form_row(deliveryForm.carriers) }}
        {{ form_end (deliveryForm) }}

    </div>
{% endblock %}
